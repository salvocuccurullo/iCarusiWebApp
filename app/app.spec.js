/*

// Not used since MoviesListController has been moved to separate component (movies-list.component.js)

describe('MoviesListController', function() {

  beforeEach(module('iCarusiWebApp'));

  it('should create a `movies` model with 3 movie', inject(function($controller) {
    var scope = {};
    var ctrl = $controller('MoviesListController', {$scope: scope});

    expect(scope.movies.length).toBe(3);
  }));

});
*/
